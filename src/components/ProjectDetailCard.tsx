import React, { FunctionComponent } from "react";

import { Box } from "@chakra-ui/react";

type ProjectDetailCardProps = {};

export const ProjectDetailCard: FunctionComponent<
  ProjectDetailCardProps
> = ({}) => {
  return <Box>ajajaj</Box>;
};
